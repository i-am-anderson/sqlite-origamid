# Estrutura

Para não ficar copiando DB em várias pastas, a partir daqui vamos usar um DB único armazenado em ```05xx-db-sqlite```, por ser um arquivo muito grande!

### Dados

Os dados desse DB foram gerados com [Faker](https://fakerjs.dev/)